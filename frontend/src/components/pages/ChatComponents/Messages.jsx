const Messages = () => {
  // const { channels, activeChannel } = useSelector((state) => state.channels);
  // const { messages } = useSelector((state) => state.messages);

  // const currentMessages = messages.filter((message) => message.channelId === activeChannel);

  // const currentChannel = channels.find((channel) => channel.id === activeChannel)

  return (
    <div>
      Messages
    </div>
  );
};

export default Messages;